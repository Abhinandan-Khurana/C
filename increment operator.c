#include <stdio.h>

int main()
{
    int i = 2;
    // printf("++i = %d\n", ++i); pehle incriment akre phir print kare..
    // printf("i++=%d\n", i++); pehle print kare phir incriment kare..
    // printf("--i=%d\n", --i); pehle decrement kare phir print kare..
    // printf("i--=%d\n", i--); pehle print kare phir decrement kare..
    // i+=10 --> increments i by 10. also -=, *=, /=, %= work.
    return 0;
}